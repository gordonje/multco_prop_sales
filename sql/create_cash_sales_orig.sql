CREATE TABLE IF NOT EXISTS cash_sales_orig (
          ZIP INT
        , date_coe DATE
        , STREET_NO INT
        , STREET_DIR VARCHAR(2)
        , STREET VARCHAR(50)
        , STREET_TYP VARCHAR(5)
        , unit_no VARCHAR(10)
        , Prop_type VARCHAR(7)
);